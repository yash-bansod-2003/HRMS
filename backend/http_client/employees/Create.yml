info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/employees/"
  body:
    type: json
    data: |-
      {
        "name": "Tony Stark",
        "email": "tonystark@avengers.com",
        "department": "IT"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
