info:
  name: Retrive
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/employees/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
